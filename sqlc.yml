version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        initialisms:
          - id
          - url